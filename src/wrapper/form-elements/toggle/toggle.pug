mixin toggle(toggleId, isChecked)
    label.toggle(for=toggleId)
        input.toggle__input(type='checkbox', id=toggleId, checked=isChecked)
        .toggle__switch
            .toggle__switch--text on
            .toggle__switch--round
            .toggle__switch--text off