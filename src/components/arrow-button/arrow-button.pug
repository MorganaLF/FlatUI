include arrow-svg-icon

mixin arrow-button(bg, direction, isDisabled, size)
  button.arrow-button(
    class="arrow-button_" + direction
    class=bg ? "arrow-button_" + bg : ""
    class=size ? "arrow-button_" + size : ""
    disabled=isDisabled
  )
    +arrow-svg-icon(direction)