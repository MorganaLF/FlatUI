mixin dropdown(options, placeholder)
  .dropdown__container
    select.dropdown.js-dropdown(name="state", data-placeholder=placeholder)
      option.dropdown__option_hidden
      each text, value in options
        option.dropdown__option_hidden(value=value)= text
